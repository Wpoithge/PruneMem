# Migration Notes

This project does not expose V1/V2/V3 as competing runtime modes.

Instead, it packages the learned system evolution:
- V1 contributed retrieval experience
- V2 contributed layered structure
- V3 contributed lifecycle governance

Public docs should present one coherent system with migration notes, not three parallel products.
